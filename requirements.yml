---
collections:
  - name: ansible.utils
    version: ">=5.1.0"
  - name: community.docker
    version: ">=5.2.0"
  - name: community.crypto
    version: ">=3.2.0"
  - name: community.general
    version: ">=12.6.0"
  - name: community.mongodb
    version: ">=1.7.12"
  - name: community.postgresql
    version: ">=3.14.3"
  - name: community.vmware
    version: ">=6.2.0"
  - name: vmware.vmware
    version: ">=2.8.0"
  - name: netbox.netbox
    version: ">=3.22.0"

roles:
  # ansible-galaxy role install only accepts exact version strings (no >= ranges).
  # Bump this pin when you want a newer release; see
  # https://galaxy.ansible.com/ui/standalone/roles/geerlingguy/docker/
  - name: geerlingguy.docker
    version: "8.0.0"
