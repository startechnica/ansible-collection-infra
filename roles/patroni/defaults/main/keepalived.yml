---
# Patroni role defaults: keepalived, the other VIP engine
# (patroni_vip_engine: keepalived).
# The role loads every file in defaults/main/ (see tasks/export_vars.yml).

keepalived_dir: /opt/keepalived
